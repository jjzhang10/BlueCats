<div class="col-md-4 " >
		<h1>
       <%= gravatar_for @applicant %>
       <div><%= @applicant.name.capitalize %></div>
       <div id="show_email"><%= @applicant.email %><div>
     </h1>
</div>

<div class="col-md-1"></div>  


<div class="col-md-7">
	<% if @applicant.admin? %>
		<h1>I'm an administrator, I can 
			<%= link_to "view all applicants", applicants_path %>.
		</h1>
	<% else %>
		<div class="jumbotron">
		<h1>
			My applications
		</h1>
		<% if @applicant.volunteers.first.nil? %>
			<p id="none">None</p>
		<% end %>
		
		<ul class="volunteers"> 
			<%= render @volunteers %>    
		</ul>
		
		<% unless current_user.admin? %>
		<%= link_to "Start new application", new_applicant_volunteer_path(@applicant), class: "center btn btn-lg btn-primary", id: "add-top" %>
		<% end %>
		</div>
	<% end %>
</div>  
