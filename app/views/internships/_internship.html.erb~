<li>
	
	 <% time=internship.created_at.strftime("%d %b %Y %H:%M")+":" %>
	 <% application='' %>
	 <% if internship.family_care %>
		<% application += " Family Care," %>
	 <% end %>
	 <% if internship.medical_internship %>
		<% application += " Medical internship," %>
	 <% end %>
	 <% if internship.ambassador %>
		<% application += " Ambassador," %>
	 <% end %>
	 <% if internship.pragram_internship %>
		<% application += " Program/Counseling Internship," %>
	 <% end %>
	 <% if internship.prevention_internship %>
		<% application += " Prevention internship," %>
	 <% end %>
	 <% if internship.additional_internship_opportunity %>
		<% application += " Additional internship Opportunity," %>
	 <% end %>
	 <% application=application[0...-1] %>
	 <span><%= time %></span>
	 <span><%= application %></span>
	 <div></div>
	 <%= link_to "View", applicant_internship_path(@applicant, internship) %>
	 <% if !current_user.nil? && !current_user.admin? && current_user?(@applicant)%>
	 	 | <%= link_to "Edit", edit_applicant_internship_path(@applicant, internship) %> 
	 	 | <%= link_to "Delete", applicant_internship_path(@applicant, internship), method: :delete, data: {confirma: "Are you sure?"} %>
	 <% end %>
	
</li>
